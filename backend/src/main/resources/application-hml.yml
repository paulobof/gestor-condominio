logging:
  level:
    root: INFO
    br.com.condominio: DEBUG
    org.hibernate.SQL: WARN

springdoc:
  api-docs:
    enabled: true
  swagger-ui:
    enabled: true

spring:
  jpa:
    properties:
      hibernate:
        format_sql: false
