# Build arg consumido pelo Vite (definir no servico frontend do Dokploy prod).
# IMPORTANTE: terminar com /api — o backend serve todos os endpoints sob /api
# e o frontend apenda os caminhos (ex: {base}/privacy/document/current).
VITE_API_BASE_URL=https://api.helbortrilogyhome.com.br/api
